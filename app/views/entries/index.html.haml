%h1 Listing Entries

- @entries.each do |entry|
  .card.mb-3(style="max-width: 540px;")
    .row.g-0
      .col-md-4
        %img.img-fluid.rounded-start(alt="..." src="https://via.placeholder.com/150")/
      .col-md-8
        .card-body
          %h5.card-title
            = entry.title
          %p.card-text
            = entry.start_date
          %p.card-text
            %small.text-muted
          %p= link_to 'Show', entry
          %p= link_to 'Edit', edit_entry_path(entry)
          %p= link_to 'Destroy', entry, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Event', new_entry_path
