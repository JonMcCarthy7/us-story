.row
  .col-12.col-sm-8
    = simple_form_for @rem do |f|
      = f.input :content, as: :text, input_html: { class: 'text-area-input' }

      .d-flex.justify-content-between
        = link_to 'Back', rems_path, class: 'btn btn-secondary'
        = f.button :submit, "Save", class: "btn-primary"
